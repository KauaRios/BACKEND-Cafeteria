# 1. Desativa a listagem de pastas
# (Se alguém acessar uma pasta sem index.php, não vai ver a lista de arquivos)
Options -Indexes

# 2. Bloqueia acesso a qualquer arquivo que comece com PONTO (.)
# Isso protege: .env, .git, .gitignore, .htaccess
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>

# 3. Bloqueia arquivos específicos do projeto que não devem ser públicos
<FilesMatch "^(docker-compose\.yml|banco\.sql|composer\.json|composer\.lock|README\.md)$">
    Order allow,deny
    Deny from all
</FilesMatch>